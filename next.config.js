/** 
 * @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.google.co.uk", "lh3.googleusercontent.com"],
  },
};

module.exports = nextConfig;
